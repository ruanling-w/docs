# Facial recognition

## OpenAPI Specification

```yaml
openapi: 3.0.1
info:
  title: ""
  description: ""
  version: 1.0.0
paths:
  /kling/v1/videos/identify-face:
    post:
      summary: Facial recognition
      deprecated: false
      description: ""
      tags:
        - Kling platform/lip-syncing
      parameters:
        - name: Content-Type
          in: header
          description: ""
          required: false
          example: application/json
          schema:
            type: string
        - name: Authorization
          in: header
          description: ""
          required: false
          example: Bearer {{YOUR_API_KEY}}
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                video_id:
                  type: string
                  description: >-
                    The ID of the video generated by Keling AI; used to specify the video and determine whether the video can be used for lip-syncing services; it is either the video_url parameter or a separate parameter, and cannot be empty or have values ​​at the same time; only videos with a duration of no more than 60 seconds generated within the last 30 days are supported.
                video_url:
                  type: string
                  description: >-
                    The URL for retrieving the uploaded video; used to specify the video and determine if it can be used for lip-syncing services; this parameter must be either the `video_id` parameter or an empty parameter; both cannot be empty or have values ​​simultaneously; video files must be .mp4/.mov, file size must not exceed 100MB, video length must not exceed 60 seconds and must not be shorter than 2 seconds, only 720p and 1080p are supported, and the width and height of each side must be between 512px and 2160px. Failure to pass the above checks will return an error code; the system will verify the video content, and if there are any problems, it will return an error code.
              x-apifox-orders:
                - video_id
                - video_url
            example:
              video_id: "827297867148050499"
              video_url: ""
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                type: object
                properties: {}
          headers: {}
          x-apifox-name: success
      security:
        - bearer: []
      x-apifox-folder: Kling platform/lip-syncing
      x-apifox-status: testing
      x-run-in-apifox: https://app.apifox.com/web/project/5443236/apis/api-386389655-run
components:
  schemas: {}
  securitySchemes:
    bearer:
      type: http
      scheme: bearer
servers:
  - url: https://api.chainhub.tech
    description: Production Environment
security:
  - bearer: []
```
