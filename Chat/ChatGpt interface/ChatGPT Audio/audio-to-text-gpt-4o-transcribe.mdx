---
title: "Audio Transcription (GPT-4o)"
api: "POST /v1/audio/transcriptions"
description: "Transcribe audio into text using GPT-4o optimized models."
---

Convert audio files into text using high-performance models like `gpt-4o-mini-transcribe`.

### Request Body

<ParamField body="file" type="file" required>
  The audio file object to transcribe. Supported formats: `flac`, `mp3`, `mp4`, `mpeg`, `mpga`, `m4a`, `ogg`, `wav`, or `webm`.
</ParamField>

<ParamField body="model" type="string" required>
  The ID of the model to use. Use `gpt-4o-mini-transcribe` or `gpt-4o-transcribe`.
</ParamField>

<ParamField body="language" type="string">
  The language of the input audio in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.
</ParamField>

<ParamField body="prompt" type="string">
  An optional text prompt to guide the model's style.
</ParamField>

<ParamField body="response_format" type="string" default="json">
  The format of the transcription output. Options: `json`, `text`.
</ParamField>

<ParamField body="temperature" type="number" default="0">
  Sampling temperature between 0 and 1.
</ParamField>

<ResponseExample>

```json Response
{
  "text": "Imagine the wildest idea that you've ever had, and you're curious about how it might scale..."
}
```

</ResponseExample>
